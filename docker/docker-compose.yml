version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - SOME_ENV_VAR=value

  frontend:
    image: nginx:alpine
    volumes:
      - ./frontend/build/:ro
    ports:
      - "80:80"
